const ChatTable = require("./chat.table");
const UserTable = require("./user.table");
const MessageTable = require("./message.table");

module.exports = {
  ChatTable,
  UserTable,
  MessageTable,
};
